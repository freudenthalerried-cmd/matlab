/**
 * Wie weit oben endet der **Kopf** eines Dokuments?
 *
 * **Der Fund, 14. September 2026, mittags.** `KOPFZEILEN` stand sechsmal in
 * diesem Haus, mit **fünf** Bedeutungen:
 *
 * | Modul | Wert | meint |
 * |---|---|---|
 * | `src/gatestand.js` | 6 | die ersten Zeilen des Gate-Registers |
 * | `src/statuskopf.js` | 6 | die ersten Zeilen von `STATUS.md` |
 * | `src/zwillingssaetze.js` | 6 | die ersten Zeilen einer Quelldatei |
 * | `src/weisungsstand.js` | 14 | so weit reicht der Kopf eines Weisungsdokuments |
 * | `src/widerruf.js` | 15 | so weit gilt ein Kopfvermerk für das ganze Dokument |
 * | `src/serverkopf.js` | — | **eine Liste von HTTP-Kopfzeilen** |
 *
 * > **Ein Name, unter dem einmal eine Zahl und einmal eine Liste steht, ist
 * > kein Name, sondern eine Verwechslung mit Anlauf.**
 *
 * Die drei Sechsen meinen dasselbe und stehen jetzt hier. Die beiden anderen
 * Zahlen meinen etwas anderes und heißen deshalb anders — sie stehen in ihren
 * Modulen, unter Namen, die ihren Gegenstand nennen. Die Liste heißt seit
 * heute `SICHERHEITSKOPFZEILEN`.
 */

/**
 * Der Kopf eines Dokuments: die ersten sechs Zeilen.
 *
 * So weit reicht in diesem Haus die Auskunft **über** ein Dokument — Titel,
 * Stand, Zahl der Einträge. Was danach kommt, ist Inhalt mit eigenen Daten,
 * und ein Prüfer, der dort weiterliest, findet das Datum einer Zeile in der
 * Mitte und hält es für den Stand des Ganzen.
 */
export const KOPFZEILEN = 6;
